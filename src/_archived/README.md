# Archived Files

This directory contains files that were removed from the active codebase during the `refactor/codebase-overhaul` branch.

**Purpose**: Safe storage instead of deletion. Original folder structure is preserved.

**When to delete**: Only after the overhaul is fully validated and merged to main.

**Reference**: See `docs/OVERHAUL-CHANGELOG.md` for the complete record of what was archived and why.
