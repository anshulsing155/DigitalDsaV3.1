---
type: reference
status: active
last_verified: 2026-06-02
owner: tech@digitaldsa.com
extracted_from: CLAUDE.md
section: "§3 — Critical Pitfalls Index"
---

# Pitfalls Index

This file holds the 69-row pitfall index that used to live in CLAUDE.md §3. Full pitfall bodies (wrong → right → why → detection → enforcement) live in [`PITFALLS.md`](PITFALLS.md). Pre-flight greps live in [`PREFLIGHT-GREPS.md`](PREFLIGHT-GREPS.md).

This file is loaded on-demand when working on a feature area whose pitfalls are relevant, NOT auto-loaded into every session — that's a deliberate cache-discipline choice (CLAUDE.md edits would bust the prompt cache; sidecar edits don't).


**Catalog moved to [`docs/PITFALLS.md`](docs/PITFALLS.md) on 2026-05-16** — CLAUDE.md was past 1,600 lines and §3 was the dominant section. Per §17 Doc Hygiene this was overdue.

When you suspect a pitfall is no longer applicable, mark it `(verified obsolete YYYY-MM-DD)` in `docs/PITFALLS.md` rather than deleting. Add new pitfalls there following the existing template (wrong → right → why → detection → enforcement → last verified).

Each pitfall is paired with a pre-flight grep in [§4 of this file](#4-pre-flight-grep-checks).

### Index

| # | Title |
| --- | --- |
| 1 | `!=` in JSON-Logic is broken for null checks |
| 2 | Missing server→client field mapping → silent failures |
| 3 | Rendering icon strings as components → full UI crash |
| 4 | `fetch` at module scope → SSR failure |
| 5 | Shared `bindsTo` keys — lock pattern |
| 6 | Sandbox git locks on NTFS mounts (Cowork Linux sandbox only) |
| 7 | `engines.node` open-ended ranges → Vercel deploys highest major |
| 8 | Returning a Capacitor proxy from an async function → `X.then() not implemented on web` |
| 9 | `typeof window !== 'undefined'` SSR guard is broken in Vite 7 |
| 10 | `state_referenced_locally` — reading reactive props inside `$state(...)` initializers |
| 11 | Snapshot fixture drift after payload-shape changes |
| 12 | Auto-clear parity across all 6 form pages |
| 13 | `combinedAnswers` underscore-split aliasing collisions |
| 14 | Numeric questions must declare explicit `minLimit` |
| 15 | `{@html}` directives must use `sanitizeHtml()` |
| 16 | Repurposed question — text override done in ONE place, drift everywhere else |
| 17 | Floating popovers (dropdown / tooltip / autocomplete) inside scrollable modals get clipped |
| 18 | Per-applicant validation that should be case-level (DC / BT / Top-up) |
| 19 | Calendar-decorated text inputs with no picker — month/year questions must declare `uiType: 'monthYear'` |
| 20 | Cross-loan applicant carryover — `formState.applicants` is global |
| 21 | Cross-field validation only fires on page navigation — `debouncedEvaluate` must be invoked |
| 22 | Stale auto-created director income entries — orphan by Company-applicant existence, not just by linkedCompanyIds |
| 23 | Don't auto-derive `hasEquity` from a missing/zero ownership — leave it for the user to answer |
| 24 | Deselecting an income profile must drop its income entries — parity across all profile-change handlers |
| 25 | Modal-saved data must persist to formState immediately — never defer to Next-click |
| 26 | Disabled Next must always have a reason — surface validation gaps to the user |
| 27 | Telemetry spans must scrub PII before export — auto-instrumentation captures more than you think |
| 28 | `@tanstack/svelte-query` v6 returns a reactive object — no `$`-prefix, no store |
| 29 | Auto-entry display fields cached at create-time + locked at write-time = stale forever |
| 30 | Component-local `$state` for "already prompted" memory resets on remount |
| 31 | Stored enum values become invalid when journey changes — without scrub the form silently passes Next |
| 32 | Cross-type restore creates ghost applicants — guard ONLY on existingSlot type silently fails for push-new |
| 33 | Plot Loan's `loanType` is the SCOPE, not the VARIANT — field-name overload across loan products  *(verified obsolete 2026-05-31 — rename closed the overload)* |
| 34 | BT applicant-structure validators that only check total count miss role-distribution drift |
| 35 | Restore modal mixed-list lets DSAs accidentally restore the wrong type |
| 36 | Restore modal must filter Individual matches by past-profile relevance for director slots |
| 37 | Director restore — soft-warn when historical companies don't include the target |
| 38 | Loan-scoped state must register with the switch chokepoint — partial cleanups guarantee stale-UI bleeds |
| 39 | ConfirmModal dismissal paths must invoke `onCancel` (X / Escape / backdrop, not just Cancel button) |
| 40 | PendingRestoreBanner Cancel must resync component buffers — formState rewind alone is silent on local copies |
| 41 | Loan variant change must reset the per-loan page index — saved index from prior variant points at a semantically different page in the new variant |
| 42 | `performance.getEntriesByType('navigation')[0].type === 'reload'` is stale across client nav — must compare entry name to current pathname |
| 43 | Sanction-letter view (property not identified) — stale property leaks into offer; key off explicit `'No'` not coerced `propertyIdentified` boolean; card count gated by `selectAffordabilityScenarios` |
| 44 | Director-in-Company income must LINK to a real Company applicant (combobox), not free-typed name — else no auto-fill + conflicting company; gate Next on same-company income |
| 45 | Business Loan applicant model — a Company is ALWAYS multi (cards+modal) via `rendersAsSingleApplicant`; sole-prop=single; flattened `businessProfilePage`/`companyFinancialsPage` retired; borrowing-firm gate at income-step not Who's-Applying |
| 46 | Director auto-income sync (`syncAutoIncomeEntries`) must follow every `commitDirectorsToApplicants` call — otherwise the Director-in-Company auto-row is never created and `sourceCompanyId` is lost (BL/Prof had the bug, HL was the reference). Locked by `directorAutoIncomeWiring.test.ts` static-scan. |
| 47 | Every loan submit MUST route through the pre-submit `confirmAndSubmit` wrapper (NOT a direct `submitFormForEvaluation` call); offer page MUST register a `beforeNavigate` guard for `/form/*` destinations. Either omission silently consumes a case slot under the incoming monthly-quota billing model. Locked by `preSubmitConfirmWiring.test.ts` static-scan. |
| 48 | Fresh worktree `pnpm install` skips `mongodb-client-encryption` native build → auth + CSFLE endpoints 500 (worktree workflows only — `main` and CI are unaffected). Workaround: `pnpm approve-builds` after `pnpm install`. |
| 49 | Entity-type switch must rewrite the persisted applicant in `formState.applicants`, not just the local form buffer — summary table / sidebar / payload reads stay stale otherwise (Pvt Ltd ↔ OPC report, 2026-05-25). |
| 50 | Per-field `minLimit/maxLimit` is insufficient for related numeric fields — need cross-field plausibility (e.g. EMI ≥ 0.9 × principal/months) to catch combinations that pass every individual bound (LAP Top-up EMI report, 2026-05-25). |
| 51 | Cross-applicant linking dropdowns (Director-in-Company, partner firm, etc.) MUST filter out already-represented entities in ADD mode — picking a duplicate target creates a parallel row instead of editing the existing one. |
| 52 | DirectorRemovePickerModal confirm must persist to `formState.applicants[Company].directors` immediately — local-buffer-only update leaks the stale pre-removal array, which resurrects the removed director on a later Previous→Next remount (specialization of #25 for the picker confirm path; BL "Tanisha reappears" report, 2026-05-26). |
| 53 | Disabled-Next reason must cover multi-applicant case-level requirements via `getCaseLevelDisabledReason()` (Pitfall #26 extension) — per-applicant `getObligationsDisabledReason` is silent when each applicant individually passes but a case-level rule (e.g. DC needs at least one closure across ALL applicants) fails. Wire BOTH helpers in every loan +page.svelte's FormNavigationBar `disabledReason` prop. |
| 54 | Long form-fill sessions must proactively refresh the JWT (`startTokenRefreshScheduler` in `(app)/+layout.svelte` onMount + `stopTokenRefreshScheduler` in `auth.logout`) — reactive-on-401 in `secureFetch` only covers API calls, not SvelteKit page navigations which hit the 401 directly and dump the user on the "Session expired" error page mid-flow. |
| 55 | `<InputField>` with `onInput=` requires `validateOnInput={true}` — `InputField.svelte:200-202` only invokes the callback when `validateOnInput` is true (default: false). Otherwise typing in the input never fires the side-effect (persistence to formState, validation, computed derivations). DOM updates via `bind:value` so the bug LOOKS like data appears but disappears on remount. Locked by `inputFieldOnInputWiring.test.ts` static-scan. |
| 56 | BL/Prof director stake % must recompute on entity-type AND director-count change — `selectEntityType` + `handleRemovePickerConfirm` MUST call `recomputeStakeAfterEntityChange()` before `commitDirectorsToApplicants` so OPC's lone director snaps to 100% (and a former-OPC 100% gets cleared on switch back). Symmetrically, `applyDirectorRestore` guard failures MUST call `restoreIntentState.reset()` — silent return leaves the Restore modal stuck open forever ("Restore button unresponsive"). Locked by `directorStakeRecompute.test.ts`. |
| 57 | Unsecured loans — `isNRI` flip MUST stash NRI-incompatible business income entries (parity with secured-loan `applyNriCleanup`). All 3 unsecured AddApplicant components (Business/Professional/Personal) MUST import `applyNriIncomeStashForApplicant` and invoke it inside `updateFormField` when `key==='isNRI'` and the value changes. Without this, director_company / business_proprietorship / business_partnership / professional_practice entries linger in `formState.applicants[id].incomeEntries` after the user flips to NRI=Yes, and the submitted payload is invalid (lenders cannot verify NRI business income). Locked by `nriIncomeStash.test.ts`. |
| 58 | Corporate Debt Consolidation — `getClosureOptionsFiltered` now takes `(applicantType, caseHasCompany)` and DROPS "Close by this loan" for Individual obligations when the case has a Company applicant on a DC route (a corporate loan cannot close personal debt). `getCaseLevelDisabledReason` now requires ≥1 obligation marked closed on a COMPANY applicant specifically (not any applicant) when the case has a Company on DC. ObligationCapture must pass both new args. Locked by `companyDCObligationGate.test.ts`. |
| 59 | Token refresh scheduler MUST fire eagerly on mount AND coalesce via the `refreshInFlight` singleton — Pitfall #54's 13-min delay before first refresh was the root cause of the persistent 401 on `/form/how-can-we-help`. `startTokenRefreshScheduler` now invokes `requestTokenRefresh()` immediately, then schedules next at T+13min. `requestTokenRefresh()` is the only public entry — `secureFetch` 401-retry uses it too. Without coalescing, scheduler + secureFetch can both POST the stale refresh token, tripping the endpoint's token-reuse detection which NUKES all sessions for the user. Locked by `tokenRefreshScheduler.test.ts`. |
| 60 | `.env` parsers silently truncate values containing `#` (comment marker) or `$` (variable interpolation) — affects every HMAC/JWT/Razorpay/MSG91 secret stored locally. D.1 S2 smoke (2026-05-26) surfaced an existing `CRON_SECRET` truncated from 40 chars to 5 by an embedded `#`, plus 4 more chars eaten by an embedded `$rw4` interpolation. Vercel + cloud env-var stores aren't affected (raw strings, no parser), but local `.env` + dotenv-style loaders ARE. Fix: regenerate secrets with hex-only chars (`openssl rand -hex 32` / PowerShell equivalent) OR wrap values in single quotes (single quotes disable both `#` and `$`; double quotes only disable `#`). Re-audit before D.1 S5 dunning + SEC-8 (both add new secrets). |
| 61 | D.1 charge cron — chargeEngine MUST query `ChargeAttempts` for a `succeeded` row on `(subscription_id, cycle_anchor)` BEFORE calling `provider.chargeMandate`. Without the probe, a cron firing twice (two Vercel regions on a deploy race, or an external scheduler retrying on 5xx) generates a fresh `attempt_id` (UUID v4) per run. Razorpay's per-receipt dedup catches the SAME `attempt_id` on retry, but NOT a different one — so the second cron run creates a second order and DOUBLE-CHARGES the DSA. The `ChargeAttempts (subscription_id, cycle_anchor)` index (S3 M1) makes the probe O(1). Also required: the pending `ChargeAttempt` row MUST be persisted BEFORE the provider call (two-phase persist per spec R1) so a crash mid-call leaves a recoverable trail for the resume path. Locked by `chargeEngineIdempotency.test.ts` (source-pattern scan that requires `probeExistingAttempt` to precede every `provider.chargeMandate` call site in the same function). |
| 62 | Income profile cards hidden by `showWhen` must auto-clear `selectedProfiles` — Pitfall #12 specialization for `IncomeProfileSelector`. When the DSA flips an answer (most commonly `isNRI=Yes`) that hides a previously-selected card via card-level `showWhen`, the card disappears from the UI but `selectedProfiles`, `applicantData.selectedProfiles`, and `incomeEntries` all keep the dropped profile. The Income Details step then enumerates `selectedProfiles` and demands an entry for a now-hidden profile with no UI to fulfil it (dead-end). Fix: a second `$effect` in `IncomeProfileSelector.svelte` filters `selectedProfiles` to drop entries whose card-level `showWhen` evaluates false (locked profiles excluded so the locked-auto-add `$effect` doesn't ping-pong) and emits via `onSelectionChange` — routes through the parent's existing cleanup cascade (`handleProfileSelectionChange` → `applicantDataStore.updateSelectedProfiles` → `softDeleteProfileEntries`). Defensive guard skips when `answersContext` is empty. Works regardless of loan family (Pitfall #57's NRI stash covered unsecured only; the secured-loan modal-gate in `applicantFormManager.svelte.ts:1574` doesn't fire in all applicant states — this is the unified safety belt). Pairs with a Yes→No reappearance toast that tracks profiles auto-dropped in the lifecycle and shows distinct messages for unsecured (auto-restored) vs secured (tap-to-restore). Locked by `incomeProfileSelectorAutoDrop.test.ts` static-scan (5 invariants: `shouldShow` import, empty-`answersContext` guard, locked-profile exemption, `shouldShow(card.showWhen, answersContext)` filter, `onSelectionChange?.(filtered)` emit). |
| 63 | Archived route folders (`_archived_*` under `src/routes/`) must still COMPILE — Rollup ignores SvelteKit's `_`-prefix privacy convention. SvelteKit's `_` prefix prevents URL registration but does NOT exclude `+server.ts` from the Vite/Rollup build graph. `@ts-nocheck` silences svelte-check (so `pnpm check` is green) but does NOT silence Rollup's import resolution — a deleted symbol referenced by an archived handler breaks `vite build` on Vercel with no local warning. Surfaced 2026-05-28 by 4 consecutive failed Vercel deploys (`_archived_da_topup` importing the retired `purchaseTopup`). Fix: every archived `+server.ts` MUST be a self-contained 410 stub importing only from `'./$types'` and `$lib/server/apiResponse.js`. Original handler is preserved via git history at the retirement SHA, not via the working tree. All four `_archived_*` folders under `src/routes/api/billing/` are now stubs (`_archived_cancel`, `_archived_da_quota`, `_archived_da_topup`, `_archived_subscribe`). Locked-by grep in §4 (CI lock-test pending). |
| 64 | Guarantor / independent-financial-applicant capacity calcs MUST sum `assessed_amount`, NEVER `final_amount`. `incomeAssessorV2.ts:146` deliberately sets `final_amount = 0` for `guarantor_financial` / `non_applicant_full_financial` sources (income is assessed independently, NOT pooled into borrower eligibility — that's the entire point of the classification). Tier 3b's first draft summed `final_amount` for the per-lender capacity check and read 0% capacity for every guarantor → universal reject. The right field is `assessed_amount` (`gross × (1 - haircut)`, per-source). Locked by `guarantorEligibilityAssessment.test.ts` source-pattern scan (`expect(block).not.toMatch(/s\.final_amount/)`). Extends to any future engine block reading non-pooled financial income (additional comfort income, co-signer visibility, etc.). |
| 65 | UX-bug investigations on auth / nav / redirect surfaces MUST grep the file for adjacent validators (sibling `isSafe*` / `safeRedirect*` / `sanitize*` / `validate*` helpers) BEFORE declaring the fix complete. Adjacent helpers may be (a) present but never called, or (b) called but weak. Discovered 2026-05-29 (`0372e6c6`): the deep-link OTP UX bug shared `login.svelte` with a legacy `isSafeRedirect` helper that was never wired to the nav site AND would have allowed `/api/...` / `//host` bypass even if it had been. A narrow UX-only fix would have left the open-redirect in place. Heuristic, not code-shape — no CI lock. Apply on any file under `src/routes/**/(login|signup|auth|onboarding)/**` or any code importing from `$app/navigation` that does `window.location.href =` or `goto(`. |
| 66 | Negative-check regexes in CI removal-locks MUST target USAGE shapes (`type Foo` / `<Foo>` / `Foo(` / `.Foo` / `Foo =`), NEVER bare identifier strings. A bare `expect(src).not.toMatch(/Foo/)` trips on the in-place removal-decision comment, on ADR references, and on JSDoc cross-refs — destroying the institutional memory the removal-lock pattern is designed to preserve. Three independent occurrences on 2026-05-29 alone (annual-billing removal lock; open-redirect bypass lock; Tier 3b `s.final_amount` lock). Test-design rule, not a grep — when reviewing any `expect(...).not.toMatch(/X/)` assertion, verify `X` is a usage shape and not a bare name that could appear in (a) removal-decision comments, (b) ADR refs, (c) cross-import paths. |
| 67 | Enricher branches in `payloadEnricher.ts` MUST read the keys the FORM actually emits (per `profileFormConfig.ts` `*_INCOME_FIELDS`), not legacy flat-field shapes — and test fixtures + synthetic data generators must mirror the live form shape or the regression hides green in CI. Surfaced 2026-05-29: `professional_practice` read `inc.netProfessionalIncome` etc., form emits `inc.financialsTable.netProfitArray` — every Professional Loan submission returned `_total_gross_monthly: 0` → no offers. Same audit fixed the empty-string filter on `business_proprietorship.netProfitArray` (current-FY `""` cell undervalued by ~25%) and added the salaried-path fallback for `director_company` / `business_partnership` (foreign-company directors / foreign-firm partners emit only `grossMonthlySalary` / `netMonthlySalary`). Test fixtures in `payloadEnricher.test.ts`, `incomeAssessorV2.test.ts`, and the generator `incomeEntryPool.ts` all used the same legacy shape — three places had to be updated to match the form. Locked by greps in §4; no static-scan yet (worth adding: every profile in `getIncomeFieldsForProfile()` must intersect with the enricher switch-case key set). |
| 68 | `CSFLE_ENABLED='true'` IS the switch — there is NO "code shipped but encryption dormant" intermediate state. `src/lib/server/csfle/client.ts:getClientEncryption()` short-circuits to passthrough ONLY when `CSFLE_ENABLED !== 'true'`. Once it's `'true'` AND `QE_LOCAL_MASTER_KEY` is valid AND the native binding loads, every `encryptValue` / `decryptValue` call is real — DEK absence throws "key not found", DEK/master-key mismatch throws `MongoCryptError: HMAC validation failure`. Both 500 fresh logins. Burned us 2026-05-18 → 2026-05-31 (13-day latent break): owner set `CSFLE_ENABLED='true'` on Vercel `rinn` (Production + Preview + Development) on 2026-05-18 07:30 IST under the wrong belief that "DEKs not initialized = no-op passthrough" (captured verbatim in `SESSION-HANDOFF.md:2210` of the 2026-05-19 morning-close block). Native binding wasn't actually loading until commit `70862a9f` (Pitfall #48 fix, this session); once it did, `encrypt()` started executing → DEKs minted 2026-05-19 with master-key A, current Vercel has master-key B → HMAC failure on every fresh login. Existing sessions kept the symptom invisible (refresh tokens never re-hit `detect-roles`). Fix: unset `CSFLE_ENABLED` on Vercel → redeploy → login back instantly. The 10 orphan DEKs in `encryption.__keyVault` are harmless until a proper SEC-2 rollout reconciles them. **Enforcement** (heuristic, no CI lock — env-state, not code-shape): (1) before flipping `CSFLE_ENABLED='true'` on any environment, run `node scripts/diagnose-csfle-state.mjs` to confirm the target cluster's `__keyVault` DEKs were minted with the CURRENT environment's `QE_LOCAL_MASTER_KEY`; (2) after the flip, do a real fresh-OTP login in incognito on the deployed environment within 5 minutes — don't trust existing sessions; (3) never trust a session-close note that says "CSFLE_ENABLED is set but DEKs aren't, so it's a no-op" — that sentence is the lie this pitfall exists to document. |
| 69 | BT+Top-up dual-tenure math is currently HARDCODED for all lenders — current engine assumes ALL BT+Top-up lenders open two separate backend loans with different tenures (dual-tenure math). Real-world treatment is per-lender and sometimes per-case: single-loan-backend lenders treat it as one combined loan (single-tenure math correct); some lenders flip conditionally on tenure split (e.g. collapse to single when base tenure fits within top-up cap). Documented but NOT fixed in S213 (D8/D9, ADR-0024) — fix requires lender-policy audit before engine change is useful. Design preserved in PITFALLS.md #69 body: extend ParsedLenderRuleDocument with bt_topup_treatment field (discriminated union: undefined defaults to dual_tenure, static string for always-one-mode lenders, {single_when: JSON-Logic} for conditional lenders). Future fix tracked in TECH-DEBT-CLEANUP-2026-05-31 §6. Heuristic detection — Select-String on Balance Transfer With Top-up outside evaluationEngine.ts. |
| 70 | Tailwind v4 escape-decoder crashes on a backslash-followed-by-6-hex-digits substring in any scanned source file. `@tailwindcss/vite@4.x` content-scans every file in the Vite build graph (`.md` AND `.ts` / `.svelte` / `.mjs` / `.css`). Leading-`--` tokens get passed to `Se()`, whose regex matches a backslash followed by 1-6 hex digits; when the 6-digit hex value exceeds `0x10FFFF` (Unicode max = 1,114,111), `String.fromCodePoint` throws `RangeError: Invalid code point` and the build aborts. Tailwind's error message points only at `src/app.css`, never the file that actually contributed the candidate. Highest-frequency vector: Windows backslash paths copy-pasted from the Claude Code auto-memory directory whose filename begins with 6 hex digits decoding above the limit. The filename `feedback_*` is a built-in landmine — its first 6 chars decode to `0xFEEDBA` (= 16,707,002). Burned us 2026-06-01 — 4 consecutive failed Vercel deploys (`52ba150` → `535e99d` → `e211595` → `3d36d6e`) traced to a markdown link in `docs/reviews/CODE-REVIEW-2026-05-30.md` line 368 pointing at the auto-memory file with backslash separators. Fix in `f6fcc7db` rewrote it as a forward-slash code-span. **Self-trigger lesson**: the first attempt at the pitfall write-up + a diagnostic probe script BOTH embedded the literal trigger sequence and crashed dev HMR — see PITFALLS.md #70 meta-note. **Detection**: `pnpm build` is the authoritative gate (catches the bug in ~15s, reports the decoded value). Grep recipes in PREFLIGHT-GREPS.md §70 are best-effort, over-inclusive triage filters. **Enforcement**: code review on doc PRs containing Windows paths; never write the literal trigger substring when documenting this pitfall — use prose ("backslash + 6 hex digits decoding to `0x<HEX>`"), `0x<HEX>` notation, and forward-slash paths. Locked-by greps in §4. |
| 71 | Form-page-level payload mutations don't reach the engine — `confirmAndSubmit` reads `formStateJson`, not the local `payload` variable. Every loan form builds a local `payload` for client-side validation then calls `confirmAndSubmit({ formStateJson: formState.toJSON(), ... })`. Mutations to `payload.<field>` after the snapshot are invisible to the engine + every downstream consumer. The 2026-05-31 Plot & Equity payload-patch reform removed two production examples; lock test `plotEquityPayloadPatchLock.test.ts` guards plot-loan against re-introduction. Other 5 loan forms not yet audited. |
| 72 | `Promise.all` in batch API routes — one bad row 500s the whole response. Endpoints that map a list through async transforms inside top-level `Promise.all` surface the first rejection as a 500 for the entire endpoint. Healthy rows are wasted. Bit production 2026-06-02 on `GET /api/cases/[id]/snapshots` when stale-ciphertext snapshots (Pitfall #68 fallout) failed decryption — entire "Load from Previous Case" modal 500'd despite plaintext payloads being intact. Fix: per-item try/catch with graceful degradation (surface failures alongside successes); use `Promise.allSettled` for batch-with-degradation patterns. Fail-loud Promise.all is correct at security boundaries (auth, payment); UX boundaries should degrade. Locked + documented in commits `eac11c29` + `dc5b614e`. Other batch endpoints (lender-applications, results, file-builder) NOT yet audited. |
| 73 | Shared modal-state leftover bleeds into newly-mounted `DatePickerYearAndMonth` instances — `MonthYearModal` is a single layout-level modal; every month-year date field talks to it via `dialogState.selectedDate` + `modalContext`. `closeDatePicker` deliberately preserves both so the post-close microtask can still flow the picked value to the active wrapper. But that leftover crosses re-mount boundaries: any DatePicker that mounts AFTERWARDS with a matching `modalContext` auto-applies the stale `selectedDate` on its first effect run — no user click required. Bit Home Loan single-applicant Income Details 2026-06-03 (`HL-2026-0071`): two business-proprietorship entries shared GST registration date; entry A's Jan-2025 silently auto-filled entry B the moment GST-registered toggled Yes, and pencil-click in edit mode silently overwrote saved entries with the last picked value. Cross-applicant slice was already protected by the `applicantIndex` routing match; same-applicant cross-entry slice bled freely. Fix: monotonic `selectionEpoch` ticks per confirmed pick; the wrapper snapshots the epoch on mount and only reacts when the epoch advances past that snapshot — leftover state becomes inert for any future mount. **Class of bug**: any global-state handshake where the writer doesn't tombstone after the reader completes. Same fix shape (writer epoch + reader mount-snapshot) applies to any future layout-level modal returning structured values through dialogState. |
| 74 | Browser-emulation libraries (`jsdom`, `happy-dom`, `linkedom`-for-full-DOM) in the SSR bundle = cold-start time bomb. When `ssr.noExternal` forces such a library to be inlined for CJS/ESM interop reasons, Vite resolves every nested `require()` in the lib's source and emits runtime-stub throws for any unresolvable optional dep (e.g. jsdom's `require("canvas")`). Those throws fire at module-init on first cold start of each function pod, killing every route that imports the affected chunk — but warm pods keep serving until they expire, masking the regression for hours. Bit production 2026-06-04 (S223): `isomorphic-dompurify` → `jsdom` → `require("canvas")` runtime-stub throw 500'd every form route. Three vite.config.ts shims failed (`85e35695` external, `2f02768e` more externals, `5261393b` resolve.alias stub — local-build showed canvas was fixed but a deeper module-init throw remained). Durable fix `6e3eff24`: replaced `isomorphic-dompurify` with `sanitize-html` (pure-JS, htmlparser2-based, ~50 KB vs jsdom's ~3 MB + transitive deps). Cleanup `76c7de73` removed all the dead workarounds. **ADR-0031** codifies the architectural rule: server-side HTML sanitization must use pure-JS libs only; no browser-emulation libraries in the SSR bundle. XSS-vector contract tests at `payloadSanitization.test.ts` ("canonical XSS vector contract", 6 vectors) lock the sanitizer's behavior. Greps in PREFLIGHT-GREPS.md §74. |
| 77 | Conflict-style detector that *classifies* without *acting* on every classification → silent verdicts accumulate ghost state forever. Pattern: pure helper partitions an input set into action buckets (`modal` / `silent` / `none`), caller acts on the loudest bucket and treats others as "expected operation, nothing to do." But "silent" usually means "user-invisible," not "no work needed." Bit SEC-10 single-session enforcement 2026-06-05 (S226): `detectConflict()` returned `'silent'` for same-browser re-login, caller short-circuited without revoking the predecessor `Sessions` row — Atlas accumulated `revoked_at == null` rows that no JWT could reach but every analytics consumer counted as "active." Owner's screenshot showed 3 admin sessions for one Android Chrome device within 70 seconds, only one revoked. Fix: `ConflictReport` refactored from single-`kind` discriminator to parallel `modal_sessions[]` + `silent_session_ids[]` arrays; gate-level `Sessions.updateMany` with `revoke_reason: 'rotated_same_browser'` runs whenever `silent_session_ids` is non-empty (failure is hygiene-only, doesn't block login). Functional security unaffected (user's cookie is rotated; old `session_id` is unreachable) — this was a data-integrity / hygiene fix, severity Medium. Lock tests: `sessionConflict.test.ts` asserts new shape + modal-coexists-with-silent case; `checkDsaConflictGate.test.ts` asserts `updateMany` call + failure-is-hygiene fallback + empty-`$in` regression guard. Class lesson: when writing classify-into-buckets helpers, prefer parallel arrays over single-kind discriminators — a discriminator's job is to tell the caller which branch to run; if multiple kinds of work can need to happen at once, a discriminator is the wrong shape. |
| 78 | `<label for={id}>` outside an array-input wrapper points at `${id}_0` not `${id}` — broken association on every render. Form-input wrapper (TextField, NumberField, etc.) renders `<label for={id}>` for the visible question; single-input mode hands `{id}` to the input verbatim (correct), but array mode (multiple inputs in one row — title-dropdown + first name, value + unit) suffixes every input as `id={`${id}_${i}`}`. Nothing in the DOM carries `id={id}`. Chrome DevTools Issues panel flags "Incorrect use of `<label for=FORM_ELEMENT>`" on every render where array mode triggers; the wrapper still functions so the bug survives past `pnpm check` + `pnpm test:unit` indefinitely. Bit Home Loan / LAP / Plot Loan property-area + applicant-name fields 2026-06-05 (S229 close, S230 fix). Companion duplicate-id bug: `DirectorCountPicker.svelte` hardcoded `id="director-count-custom"` collides when two pickers mount on the same page (multi-director company forms). Fix: array-mode first input gets `id={i === 0 ? id : `${id}_${i}`}` (label-for now matches; `name=` attributes unchanged so form-data and autofill are untouched); DirectorCountPicker takes optional `id` prop with module-scope counter fallback, custom input derives `${id}_custom`, container wrapped in `role="group"` + `aria-labelledby` pointing at the visible heading. Lock test `formLabelIdAssociation.test.ts` source-pattern-scans the 3 wrapper components + QuestionRenderer caller; companion preflight greps PREFLIGHT-GREPS §78. Class lesson: wrapper components that contain BOTH a `<label for=>` and an `<input id=>` are responsible for keeping the two in lockstep — never let array-mode and single-mode drift their id schemes independently. |

---

